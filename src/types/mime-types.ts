/**
 * All possible mime types that are automatically detected.
 */

export enum MimeTypes {
  HTML = 'text/html',
  CSS = 'text/css',
  JS = 'text/javascript',
  JSON = 'application/json',
  TEXT = 'text/plain',
}
