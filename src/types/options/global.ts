/**
 * Options that affect all intercepts globally,
 * passed when instantiating the Intercept class.
 */

export interface GlobalOptions {
  fixturePathPrefix?: string;
  staticExtensions?: string[];
}
